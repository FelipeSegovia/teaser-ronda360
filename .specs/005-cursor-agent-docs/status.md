Estado: done
Última tarea completada: T4 — cierre
Siguiente: nada

## Qué se hizo

- T1 — `landing/AGENTS.md` canónico: título `# AGENTS.md`, reglas de
  `CLAUDE.md` (stack, fidelidad, idioma, SDD) y nota de fuente para Cursor.
- T2 — `landing/CLAUDE.md` reducido a puntero hacia `AGENTS.md`.
- T3 — referencias de `001-landing-early-access` (spec, plan, tasks) apuntan
  a `AGENTS.md`.
- T4 — grep: en `001` no queda `CLAUDE.md`; fuera de node_modules el archivo
  `landing/CLAUDE.md` es solo el puntero. Raíz `CLAUDE.md` y
  `docs/constitution.md` sin cambios.

## Desvíos respecto del plan

Ninguno. El implement del plan de Cursor se tomó como OK de spec → plan →
tasks para esta spec.
