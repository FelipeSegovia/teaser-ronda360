# El comentario de ejemplo en `.env.example` puede coincidir con el valor
# real de RESEND_FROM en Netlify; el secrets scanning lo trata como leak.
[build.environment]
  SECRETS_SCAN_OMIT_PATHS = ".env.example"
